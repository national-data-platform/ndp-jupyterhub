
# Sample overrides for the JupyterHub Makefile. Copy this file to `config.mk`
# and adjust the values to match your deployment environment.

KUBE_CONTEXT =
HELM_RELEASE = ndp-jhub
NAMESPACE = ndp-jhub